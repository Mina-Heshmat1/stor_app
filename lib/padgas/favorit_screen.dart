import 'package:flutter/material.dart';

class FevoritUiScreen extends StatelessWidget {
  const FevoritUiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('FevoritUiScreen'),
      ),
    );
  }
}
