import 'package:flutter/material.dart';

class UserProfail extends StatelessWidget {
  const UserProfail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
